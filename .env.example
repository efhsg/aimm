# Linux/WSL: set USER_ID to `id -u` and USER_NAME to `id -un`.
# macOS/Windows: defaults are usually OK.
USER_ID=1000
USER_NAME=appuser

TIMEZONE=Europe/Amsterdam

# Host ports (inside Docker network: Nginx is 80, MySQL is 3306)
NGINX_PORT=8510
DB_PORT=3308

# Internal PHP-FPM port (Nginx -> PHP-FPM only)
PHP_FPM_PORT=9000

# Database (dev only)
DB_HOST=aimm_mysql
DB_ROOT_PASSWORD=root_secret
DB_DATABASE=aimm
DB_DATABASE_TEST=aimm_test
DB_USER=aimm
DB_PASSWORD=aimm_secret

# Optional: admin user for DB GUI tools (dev only). Granted full rights on DB_DATABASE + DB_DATABASE_TEST.
DB_ADMIN_USER=admin
DB_ADMIN_PASSWORD=admin_secret

# Yii web (dev only)
COOKIE_VALIDATION_KEY=dev-only-change-me

# Admin UI credentials (industry-config management)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# Xdebug (optional)
XDEBUG_MODE=off
# CLI example:
# XDEBUG_CONFIG="client_host=host.docker.internal client_port=9878 idekey=PHPSTORM"

FMP_API_KEY=your_fmp_apikey_here
